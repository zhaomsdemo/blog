spring:
  application:
    name: blog
  data:
    mongodb:
      database: mini-blog
      uri: mongodb://localhost/mini-blog
  mvc:
    servlet:
      path: /api

logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG
    org.springframework.data.mongodb.repository.query: DEBUG
